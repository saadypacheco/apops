# Icons placeholder

Generar `icon-192.png` (192×192) y `icon-512.png` (512×512) con el isotipo de
APOPS antes del build de producción. Los iconos NO se generan en esta feature
(quedan como tarea pendiente del kit de branding).

Especificaciones:
- Fondo: `#042C53` (navy)
- Iso: blanco / clear
- Padding seguro para `purpose: "maskable"` (mínimo 10% en cada lado)
- Formato PNG, sin transparencia para maskable
